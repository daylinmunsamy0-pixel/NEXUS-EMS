// js/config.template.js
//
// Committed to git as a TEMPLATE ONLY — contains no real values.
// Netlify's build command substitutes the placeholders with the actual
// environment variables at deploy time.
//
// For local development: copy this file to js/config.js yourself and
// fill in your STAGING project's Project URL and Publishable key.
// js/config.js is gitignored — never commit real keys.

export const SUPABASE_URL = "__SUPABASE_URL__";
export const SUPABASE_KEY = "__SUPABASE_KEY__";
